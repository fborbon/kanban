{
  "name": "scrum-canva",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "NODE_OPTIONS=--experimental-global-webcrypto tsc && NODE_OPTIONS=--experimental-global-webcrypto vite build",
    "preview": "vite preview",
    "deploy:web": "BASE_URL=/ NODE_OPTIONS=--experimental-global-webcrypto tsc && BASE_URL=/ NODE_OPTIONS=--experimental-global-webcrypto node_modules/.bin/vite build && aws s3 sync dist/ s3://scrum-forwardforecasting-295936871972/ --delete --exclude 'uploads/*' --exclude 'drive/*' && aws cloudfront create-invalidation --distribution-id E1AQDWOGNRBTEY --paths \"/*\" && echo '✅ Kanban deployed to https://kanban.forwardforecasting.eu/'"
  },
  "dependencies": {
    "@hello-pangea/dnd": "^17.0.0",
    "react": "^18.3.1",
    "react-dom": "^18.3.1"
  },
  "devDependencies": {
    "@types/react": "^18.3.3",
    "@types/react-dom": "^18.3.0",
    "@vitejs/plugin-react": "^4.3.1",
    "typescript": "^5.4.5",
    "vite": "^5.3.1",
    "vite-plugin-pwa": "^1.3.0"
  }
}
